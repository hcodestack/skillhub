---
name: pdf-tools
description: |
  Extract text and tables from PDF files, merge and split documents.
  Use whenever the user mentions a .pdf file. This description is written as a
  YAML block scalar on purpose — the hub must parse it correctly.
---

# PDF Tools

Use `pdfplumber` for text extraction and `pypdf` for page operations.

## Setup

pip install pdfplumber pypdf
